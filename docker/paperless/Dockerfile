# Imagem oficial Paperless-ngx (web + consumidor + workers no mesmo contentor).
# Na Railway: Settings → Config-as-code → /docker/paperless/railway.toml
FROM ghcr.io/paperless-ngx/paperless-ngx:latest
