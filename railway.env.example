# Variáveis típicas na Railway — API GED (exemplo; preferir Reference Variables do painel).

DATABASE_URL=
JWT_SECRET=
CORS_ORIGIN=

# Paperless-ngx — URL raiz (sem /api). Autenticação: token OU utilizador/senha.
PAPERLESS_API_URL=
PAPERLESS_TOKEN=
PAPERLESS_USERNAME=
PAPERLESS_PASSWORD=
